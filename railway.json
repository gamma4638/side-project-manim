{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "dockerfilePath": "linear-algebra-visualizer/Dockerfile"
  },
  "deploy": {
    "startCommand": "uvicorn backend.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
